#include <iostream>
#include <vector>

int main()
{
    std::vector<int> foo(10);
    int idx = -1;
    int start_idx = -1;
    if (idx >= start_idx + foo.size()) {
        std::cout << "You gotta be kidding me!\n";
    }    
}
