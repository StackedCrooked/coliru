Thread Thread Thread 1runs before barrier0runs before barrier2runs before barrierThread 4runs before barrier


Thread 3runs before barrier

Thread 1runs after barrier
Thread 2runs after barrier
Thread 0runs after barrier
Thread 3runs after barrier
