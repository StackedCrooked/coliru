module Standard {
    module Algorithms {
        None(pred) {
            return function(range)[pred := move(pred)] {
                return All(function(val)[pred := move(pred)] { return ~pred(val); })(move(range));
            };
        }
    }
}