module Standard {
    module Algorithm {
        Drop(count) {
            return function(range)[count := count] {
                return function()[range := move(range), begin := 0] {
                    while(begin ~= count) {
                        range();
                        ++begin;
                    }
                    return range();
                };
            };
        }
    }
}