module Standard {
    module Algorithm {
        ForEach(fun) {
            return function(range)[fun := move(fun)] {
                var := range();
                while(var) {
                    fun(*move(var));
                    var = range();
                }
            };
        }
    }
}