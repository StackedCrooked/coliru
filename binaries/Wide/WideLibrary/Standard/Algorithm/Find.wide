module Standard {
    module Algorithm {
        Find(func) {
            return function(range)[func := move(func)] {
                var := range();
                while (var) {
                    if (func(*var)) return move(var);
                    var = range();
                }
                return Containers.None;
            };
        }
    }
}