module Standard {
    module Range {
        StreamInserter(stream := auto.lvalue) {
            return function(val)[&] { stream << move(val); };
        }
        StreamInserter(stream := auto.rvalue) {
            return function(val)[stream := move(stream)] { stream << move(val); };
        }
    }
}