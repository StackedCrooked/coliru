module Standard {
    module Range {
        Delimited(begin, end) {
            return function()[begin := move(begin), end := move(end)] {
                if (begin ~= end) return Containers.Optional(decltype(begin).decay)(begin++);
                return Containers.None;
            };
        }
    }
}