#include <cmath>
#include <iostream>
#include <limits>

void changeStr( std::string (&strings)[10] )
{
   // access strings[i] for i >= 0 and i < N
}

int main()
{
    std::string s[3];
    changeStr(s);
}
