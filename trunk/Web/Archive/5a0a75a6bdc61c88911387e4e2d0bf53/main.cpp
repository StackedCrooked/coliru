#include <iostream>
#include <memory>
#include <map>
#include <vector>
using namespace std;

using TDataMap = std::map<std::string, std::unique_ptr<std::vector<unsigned char>>>;

int main() {

}
