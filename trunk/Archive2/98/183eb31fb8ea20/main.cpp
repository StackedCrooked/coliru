/*
template <typename Key>
void DoublyLinkedList<Key>::clear()
{
    Node* victim = Head_;
    while (victim && victim->next != Tail_)
    {
        if (victim == Head_)
            victim = victim->Next_;
            
        Node* temp = victim;
        victim = victim->Next_;
        delete temp;
    }
    Size_ = 0;
}
*/

int main()
{
    
}