#include <iostream>
#include <vector>

int main()
{
    std::vector<double> arr1, arr2, arr3, arr4;
    
    std::vector<std::vector<double>> coordinates;
    coordinates.push_back(arr1);
    coordinates.push_back(arr2);
    coordinates.push_back(arr3);
    coordinates.push_back(arr4);
}