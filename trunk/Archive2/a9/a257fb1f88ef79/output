
main.cpp:4:1:
    Non type-variable argument
      in the constraint: MonadState CacheType m
    (Use -XFlexibleContexts to permit this)
    When checking the class method:
      iDon'tKnow :: forall (m :: * -> *).
                    (MonadState CacheType m, MonadIO m) =>
                    t -> m ()
    In the class declaration for `SomeClass'
