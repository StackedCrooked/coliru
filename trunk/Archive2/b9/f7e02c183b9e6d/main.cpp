#include <iostream>
#include <string>
#include <vector>

void foo() {}

int main()
{
    double alpha, beta = 20, gamma = 2;
    alpha = beta / gamma * 1,8;
    std::cout << alpha <<  std::endl;
    return 0;
}
